CREATE TABLE tweet (
    id double ,
    follewersCount int,
    email VARCHAR(30),
    tag VARCHAR(30),
    auteur VARCHAR(50),
    libelle TEXT,
    PRIMARY KEY (id)

);
 ENGINE = InnoDB;
 DEFAULT CHARSET = utf8;